# SQLite 데이터베이스는 애플리케이션 서버의 로컬 디스크에 둡니다.
# 상대 경로는 프로젝트 루트를 기준으로 합니다.
DATABASE_URL="file:./data/app.db"

# 사내 HTTP 접속은 false, HTTPS reverse proxy 적용 후 true로 변경합니다.
SESSION_COOKIE_SECURE="false"
SESSION_TTL_HOURS="12"

# 담당자용 간단 사용 안내

## 1. 로그인

관리자가 발급한 주소와 계정으로 로그인한다. 같은 계정을 함께 쓰지 않는다.

- ADMIN: 사용자, 공급자, 데이터 이관 포함 전체 관리
- MANAGER: 마스터·계약·매출 등록과 확정·발행
- VIEWER: 조회와 export·재출력

## 2. 현장·품목 마스터

한 건씩 추가·수정하거나 Excel 파일과 Excel 복사·붙여넣기로 대량 입력할 수 있다. import 전에 신규·수정·동일·오류 preview를 확인한다.

별칭에는 실무에서 부르는 다른 이름을 넣는다. 문장으로 빠른 입력이 이 별칭을 사용한다.

### 목록 정렬

현장, 품목, 계약, 매출 원장과 월마감의 데이터 헤더를 누르면 `오름차순 → 내림차순 → 기본 순서`로 바뀐다. 검색·필터가 적용된 전체 결과를 먼저 정렬하므로 다음 페이지에서도 순서가 이어진다. 선택 체크박스와 관리 버튼, 팝업 안의 보조 표는 정렬되지 않는다.

현장·품목·계약·매출 원장은 최종수정일 최신순이 기본이며 현장과 품목 표에서도 최종수정일을 확인할 수 있다. 월마감은 열린 현장, 차단 예외 수, 전체 예외 수 순의 업무 우선순위가 기본이다. 선택한 정렬은 검색·필터와 함께 주소에 함께 저장되므로 새로고침, 뒤로가기와 공유 링크에서도 같은 조건을 다시 연다.

## 3. 계약

현장, 상태와 하나 이상의 품목 행을 입력한다. 계약 전체 기간은 품목별 청구 기간에서 자동으로 정해진다.

- 표준단가는 품목 마스터 값이다.
- 현장 협의나 A/S 단가는 직접 바꾸고 사유를 적는다.
- 대부분의 반복 사용료는 `월정액`을 선택하고 시작월·종료월을 입력한다. 선택한 모든 월에 수량×단가 전액이 발생한다.
- 일부 기간만 총액을 나누어 청구할 때만 `일할청구`를 선택하고 실제 시작일·종료일을 입력한다.
- 일할청구는 같은 달 또는 서로 붙어 있는 두 달까지만 등록할 수 있다. 세 달에 걸치면 기간을 나누어 등록한다.
- 기존 계약에 `기존 계산 유지`가 표시되면 방식을 직접 바꾸기 전까지 과거 계산 기준이 유지된다.

문장으로 빠른 입력:

1. 문장을 입력하고 분석한다.
2. 현장·품목·수량·기간·단가와 자동 선택된 `월정액` 또는 `일할청구`를 확인한다.
3. 선택 필요 항목을 직접 고른다.
4. 등록 폼에 적용한다.
5. 폼 값을 다시 확인하고 저장한다.

분석 또는 폼 적용만으로 저장되지는 않는다.

## 4. 매출 원장

계약 매출 생성은 처리할 내용이 남아 있는 진행 계약만 보여준다. 현장이나 계약번호·계약명으로 검색하고, 계약 선택, 미리보기 확인, 생성·갱신 순서로 진행한다. 미리보기의 `청구 근거`에서 월정액 전액, 일할 사용일수 또는 기존 계산 배분을 확인한다. 이미 동기화된 계약은 목록에서 사라지며 확정 매출은 계약을 바꿔도 자동으로 덮어쓰지 않는다.

직접 매출:

- 품목·수량·단가를 넣어 계산형으로 등록할 수 있다.
- 품목 없이 제목과 최종 매출액만으로 자유형 등록할 수 있다.
- 계산값과 직접 금액이 다르면 예외 사유를 적는다.
- 음수 금액은 조정 유형과 조정 사유를 사용한다.

작성 중 매출은 수정 가능하다. 확정 후에는 취소 이력을 남긴다.

## 5. 월별 현황과 메모

기간과 현장을 선택하면 현장별 월 청구액을 확인할 수 있다. 월 금액에 마우스를 올리거나 누르면 원장 상세가 표시된다.

현장·월 메모에는 특이사항, 청구 이슈, 담당자 공유 내용을 적는다. 다른 담당자가 먼저 수정하면 충돌 안내가 나온다.

Excel export에는 월별 요약, 매출 상세, 월별 특이사항이 포함된다.

## 6. 월마감

거래명세표를 발행하기 전에 `월마감`에서 현장별 해당 월 매출을 확인하고 마감한다.

1. 매출월을 선택한다.
2. `예외만`에서 계약 기준과 다른 금액, 계약에 없는 직접 입력, 작성 중 매출, 기존 발행 후 변경을 확인한다.
3. 계약과 다른 단가나 직접 입력 건은 `검토`를 눌러 인지·승인 사유를 남긴다.
4. 작성 중 매출은 매출 원장에서 확정하거나 취소한다.
5. 차단 예외가 없으면 현장별 `마감` 또는 `가능한 현장 모두 마감`을 누른다.
6. 마감된 현장의 `발행으로 이동`에서 거래명세표를 발행한다.

마감된 현장·월의 계약과 매출은 수정할 수 없다. 20일 이후 추가 계약이나 수정 건이 생기면 관리자가 `마감 되돌리기`로 다시 연다. 사유를 기록하고 계약·매출을 수정한 뒤 예외 검토와 재마감을 다시 수행한다. 기존 거래명세표와 재마감 결과가 다르면 월마감에 `대체 발행 필요`가 표시된다.

## 7. 거래명세표

### 공용 템플릿 만들기

관리자와 매니저는 `거래명세표 > 템플릿 관리`에서 시스템 기본을 복제해 공용 템플릿을 만든다. 시스템 기본은 변경하거나 삭제할 수 없다.

- A4 미리보기의 블록을 끌어 이동하고 오른쪽 아래 손잡이로 크기를 조절한다. 방향키는 이동, `Shift + 방향키`는 크기 조절이다.
- 선택한 블록의 글꼴, 크기, 굵기, 글자색, 배경색과 테두리색을 바꿀 수 있다.
- 품목표 열의 표시 여부, 너비 비율과 순서를 바꿀 수 있다. 표시 중인 열 비율의 합계는 자동으로 100% 기준으로 환산되며, 품명과 금액 열은 항상 표시된다.
- 블록은 A4 격자에 맞춰 움직이며 페이지 밖이나 다른 블록 위로 이동할 수 없다.
- 저장 전 경고가 보이면 블록 겹침이나 A4 경계 등 표시된 항목을 수정한다.

공용 템플릿은 업체별로 자동 연결되지 않는다. 필요한 BI 색상 양식을 별도 이름으로 만들어 발행할 때 직접 선택한다. 조회 사용자는 템플릿을 볼 수 있지만 변경할 수 없다.

### 발행하기

1. 매출월을 선택하고 `후보 조회`를 누른다.
2. `발행 대기`에서 `신규`와 `대체` 대상을 확인하고 필요한 항목을 선택한다. `전체 선택`은 두 종류를 모두 포함한다.
3. 동일 품목 합산 또는 원장 건별 표시를 고른다.
4. 출력 템플릿을 고른다.
5. 실제 A4 미리보기에서 전체·신규·대체 건수와 대체될 현재 발행번호, 배치와 자동 페이지 나눔을 확인한다.
6. 발행한다. 일부 대상이 변경되었으면 나머지는 발행되고 실패 대상만 사유와 함께 대기에 남는다.
7. 이력에서 snapshot을 재출력한다. 이력 행의 `대체 발행`도 단건 처리 진입점으로 계속 사용할 수 있다.

발행일과 매출월은 별도다. 5월분을 5월 20일에 마감하고 발행할 수 있다. 발행은 마감 snapshot 전체를 사용하므로 개별 매출을 빼거나 더할 수 없다. 현재 유효본에 포함된 마감 회차는 중복 발행할 수 없다. 발행 당시 템플릿 전체도 snapshot으로 보존되므로 이후 원본 템플릿을 수정하거나 삭제해도 재출력 모습은 바뀌지 않는다.

### 추가 계약이 생겼을 때 월 전체 대체 발행하기

1. 관리자가 월마감에서 해당 현장·월의 `마감 되돌리기`를 누르고 사유를 입력한다.
2. 추가 계약의 해당 월 매출을 `계약 월 매출 생성`으로 만들고 확정한다. 계약만 등록하면 거래명세표에 포함되지 않는다.
3. 월마감에서 새 예외를 검토하고 해당 현장·월을 다시 마감한다.
4. 월마감의 `대체 발행 필요` 표시를 확인한다.
5. `거래명세표 > 발행 대기`에서 해당 현장의 `대체` 항목을 선택해 다른 신규·대체 건과 함께 발행한다. 단건 처리하려면 발행 이력의 `대체 발행`을 누른다.
6. 새 발행일, 표시 방식, 출력 템플릿과 메모를 확인하고 미리보기를 누른다.
7. 재마감된 매출 전체와 대체될 현재 발행번호를 확인한 뒤 발행한다.

대체 발행이 완료되면 새 문서만 `유효`로 표시되고 이전 문서는 `대체됨` 이력으로 남는다. 대체된 과거본도 재출력할 수 있지만 문서 위쪽에 최신 발행번호와 함께 `대체된 과거 발행본`이 표시된다. 추가 매출이 다시 생기면 새 유효본에서 같은 절차를 반복한다.

이 시스템의 업무 범위는 월마감과 거래명세표 발행까지다. 외부 전자세금계산서 발행은 연동하거나 관리하지 않는다.

## 8. 실시간 협업

다른 담당자가 저장하면 관련 목록·현황이 자동 갱신된다. 동시에 같은 데이터를 수정해 충돌 메시지가 나오면 새로 불러온 뒤 다시 수정한다.

## 9. 기존 데이터 이관

관리자만 사용한다. 먼저 backup을 만들고 JSON 또는 Excel preview의 오류·중복·합계를 확인한다. 검증 보고서를 내려받은 뒤 확정한다.

같은 파일명이라도 실제 데이터가 같으면 재이관이 차단된다. 오류가 있으면 원본을 수정하고 다시 preview한다.

## 10. 문제가 생겼을 때

- 로그인 불가: 관리자에게 계정 활성 상태 확인
- 합계 불일치: 기간·현장·상태 filter와 취소 건 확인
- Excel 오류: template header와 날짜·숫자 형식 확인
- 저장 충돌: 화면을 새로 불러오고 최신 값 기준 재입력
- 전체 접속 불가: server 담당자에게 /api/health와 Windows task 확인 요청
- 복구 필요: 사용자 임의 처리 금지, 운영 담당자가 OPERATIONS_GUIDE.md 절차 수행
